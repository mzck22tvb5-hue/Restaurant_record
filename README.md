# 我的餐廳地圖（行動版 v1 / Places API v1）
1. 在 `index.html` 的 script 以及 `app.js` 的 `GOOGLE_API_KEY` 都換成你的 API Key。
2. 在 Google Cloud Console 啟用：Maps JavaScript API、Places API (New/v1)。
3. 若有限制金鑰，HTTP 來源加入 `https://你的帳號.github.io/*`。
